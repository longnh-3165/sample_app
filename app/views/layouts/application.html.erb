<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title yield(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
    <%= link_to t("application.en"), locale: :en %>
    <%= link_to t("application.vi"), locale: :vi %>
    <%= yield %>
  </body>
</html>
